server {
    listen       80;

    charset utf-8;
    root /var/www/;
    index  index.html index.htm;

    location / {
        try_files /_not_exists_ @backend;
    }

    location @backend {
        proxy_pass http://golden-bag-backend_a:8080;
    }
}
